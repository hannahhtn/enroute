/** @format */

import { Button } from "@mui/material";

const Date = () => {
  return (
    <Button variant="contained" color="white" size="small">
      Date{" "}
    </Button>
  );
};

export default Date;
